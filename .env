# Ścieżki hosta (mapowane do kontenerów)
UPLOADS_DIR=./data/uploads
OUTPUTS_DIR=./data/outputs
INSIGHTFACE_HOME=./models_cache

# CORS (dla trybu dev / frontu)
# Możesz zawęzić do konkretnych originów:
# CORS_ALLOW_ORIGINS=http://localhost:8080,http://localhost:5173
CORS_ALLOW_ORIGINS=*
